services:
  app:
    build: .
    depends_on:
      - redis
    env_file: .env
    environment:
      REDIS_HOST: redis
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    restart: unless-stopped
